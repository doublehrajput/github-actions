name: first

on:  
  push:
    branches: [ master ]

jobs: 
  job1:
    name: First Job
    runs-on: ubuntu-latest
    steps: 
    - name: Step One
      uses: actions/checkout@v2
    - name: Step Two 
    - run: echo "Checking ubuntu version"
      run: lsb_release -d


